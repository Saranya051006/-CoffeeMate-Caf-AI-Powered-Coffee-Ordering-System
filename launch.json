{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Run CoffeeMate Agent",
            "type": "python",
            "request": "launch",
            "program": "${workspaceFolder}/coffee_agent.py",
            "console": "integratedTerminal",
            "envFile": "${workspaceFolder}/.env"
        }
    ]
}
